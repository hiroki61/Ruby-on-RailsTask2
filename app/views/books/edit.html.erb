<h1>Editing Book</h1>
<%= form_for(@book) do |f| %>
    <p>Title<p>
    <%= f.text_field :title %>
    <p>Body<p>
    <%= f.text_area :body %>
    <p> </p>
    <%= f.submit 'Update Book' %>
    <%=link_to 'Show' , "/books/#{@book.id}" %>
    <%=link_to 'Back' , "/books" %>
<% end %>
